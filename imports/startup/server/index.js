import '../../api/Users/UsersCtrl';

/*import {ValidatedMethod} from 'meteor/mdg:validated-method';
import {check} from 'meteor/check';

Meteor.methods({
    testMethod(){
        console.log('Hola Mundo');
        return 'this is an axample';
    },

    connectionData(){
        this.unblock();

        console.log('context method:', this);
    }
});

new ValidatedMethod({
    name: 'multiplication',
    validate({a,b}){ //validate:null
        check(a , Number);
        check(b, Number);
    },
    run({a,b}){
        return {result: a*b};
    }
});*/